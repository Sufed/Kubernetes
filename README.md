git init

git remote add origin https://github.com/ТВОЙ_USERNAME/Kubernetes.git

git checkout -b Start-nginx-site

git add .

git push -u origin Start-nginx-site

Another
------------------

git add .

git commit -m "Multiple sites, and main site."

git push origin Start-nginx-site
